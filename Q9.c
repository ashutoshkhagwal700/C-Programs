// program to calculate area of rectangle
#include<stdio.h>
int main(){
    int l,b,area;
    printf("enter l and b");
    scanf("%d%d",&l,&b);
    area=l*b;
    printf("area is %d",area);
    return 0;
}